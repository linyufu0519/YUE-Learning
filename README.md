# 林小玥六年級數學學習站（康軒版為主，翰林版保留）— MVP

一個**完全離線、免後端**的國小六年級數學學習網站。網站預設使用**康軒版**課本目錄，並完整保留**翰林版**的所有內容與學習紀錄，使用者可隨時在首頁切換版本。使用純 HTML / CSS / JavaScript（ES Modules）打造，適合直接部署到 GitHub Pages。

> 👧 小朋友第一次使用，可以先看：[**給玥玥的 79 關使用說明**](./docs/玥玥的79關使用說明.md)

## 功能總覽

- 📚 **教材版本選擇**：首頁上方提供「康軒版 / 翰林版」切換按鈕，**預設康軒版**。切換後會保存到 `localStorage`（若已登入雲端同步，也會一併同步版本偏好），首頁單元卡片、今日任務、教學入口、練習入口與家長進度頁的單元列表都會依所選版本重新顯示；兩個版本的學習紀錄各自獨立累計，互不污染，且切回翰林版時原有紀錄完整可用。
- 🗺️ **康軒六上 79 關學習地圖**：依 11 大項分成 79 關，單元內依序解鎖；每關包含教學與自我檢查、10 題練習、全對或修正來源錯題三段任務，可自由安排進度，不綁日期或預測完成日。首頁可按單元展開查看鎖定、可開始、進行中與完成狀態。
- 🎲 **關卡專屬動態題庫**：題目依 79 個關卡的實際主題產生，不再由同單元共用固定模板。例如「質數與合數」會做質合數判斷，「公因數」只練共同整除，「連比」會出三量題；每關至少有三種運算策略。題目使用穩定的隨機參數及人物、物品、地點情境組合，easy 為直接辨識／計算、medium 為反推／換算、hard 為兩步驟生活應用。
- 🏠 **首頁**：目前學習關卡、錯題複習入口、79 關整體進度、連續學習天數、康軒六上學期 XP、徽章、等級稱號與**等級獎品**。稱號依等級為：Lv.1～10「玥玥剛出新手村」、Lv.11～30「數學難不倒我」、Lv.31～50「數學小老師」、Lv.51～99「數學天才」、Lv.100「數學大師玥玥」。
- ✖️➗ **已可練習單元**：翰林版「分數的乘法」「分數的除法」、康軒版「第1單元 最大公因數與最小公倍數」「第2單元 分數除法」「第3單元 數量關係」「第4單元 小數除法」（第2單元沿用同一套分數除法動態題庫，但學習紀錄依版本分開累計）使用動態題目引擎，每單元至少 30 題以上，練習時每次隨機抽 10 題，涵蓋選擇題與輸入題，具備：
  - 難度分級：基礎（easy）、進階（medium）、挑戰（hard）
  - 智慧練習：依目前正確率自動選擇適合難度
  - 避免短時間重複出現同一題
  - 立即批改與正確/錯誤標示
  - 提示（Hint）與詳解（解析）
  - 答題進度條與即時正確率
  - 練習結束的結算畫面（答對題數、正確率、單元完成度）
- 📝 **錯題複習**：首頁提供學生可直接進入的錯題複習入口，依目前教材版本列出錯題及所屬單元，可選擇重答單一題目或整份錯題。選擇題會保留原選項，輸入題維持輸入作答；舊錯題若尚未保存題型，會依版本、單元與題號回查原題庫還原。重新答對會從該版本錯題本移除並計入「修正錯題」每日任務；再次答錯則更新最新答案與日期並保留。錯題本為空時顯示明確完成提示。資料仍沿用既有版本化 `wrongBook` 結構，可與舊 localStorage 及 Firebase 合併資料相容。
- 📖 **教學模式**：所有單元卡片都有「先學習／教學模式」入口。分數乘法、分數除法與康軒第1～4單元提供概念、步驟、常見錯誤、例題解析、自我檢查；康軒版第5～9單元與複習（一）（二）（依課本目錄建立，僅列出單元／小節標題，不含課本文字或題目）與翰林版其他單元皆提供學習目標與小節重點的基礎教學導覽，避免只顯示敬請期待。
  - ✅ **自我檢查改為勾選制**：每個自我檢查項目都是一個 checkbox，**必須全部勾選**才能啟用「我讀完了」按鈕；未勾完前按鈕維持 disabled 無法送出。點下「我讀完了」才會記錄該單元教學閱讀完成；若當日尚未完成閱讀任務，會一次取得該任務的 25 XP。若單元沒有自我檢查項目（例如未來新增單元忘記設定），會自動視為可直接完成，不會卡住使用者。每次重新載入教學頁，勾選框都會重置為未勾選（避免誤顯示「本次已完成」），但只要**曾經**完成過這個單元的自我檢查，頁面上方會顯示「已完成過」提示，方便複習。
- 🚀 **關卡 XP 與單元獎勵**：康軒每關教學 25 XP、10 題練習 50 XP、全對或修正本關錯題 25 XP，合計 100 XP；三段與單元完成獎勵都只能取得一次，重複複習不重複加分。79 關共 7900 XP，9 個正式單元各 200 XP、兩次複習各 100 XP，共 2000 XP，學期總上限 9900 XP；從 0 XP 全部完成剛好 Lv.100「數學大師玥玥」。既有 `rewards.xp` 保留為歷史資料，不回溯、不刪除；新 UI 主要顯示獨立的六上學期 XP。星星資料仍保留相容但 UI 不顯示。
- 🎁 **等級獎品（零用錢里程碑）**：每 10 級一個獎品里程碑（Lv.10～100），10～40 級與 60～90 級各為零用錢 100 元，**Lv.50 與 Lv.100 為零用錢 500 元**；達到 100 級為目前最高里程碑，會顯示「目前最高獎勵里程碑為 100 級」。里程碑是否**解鎖**依目前等級即時判斷；是否**已確認發放**則由家長在家長頁（需先通過 8888 密碼）按下「確認領取」才會標記，代表零用錢已實際交給孩子——**小朋友端無法自行標記已領取**，避免誤操作。首頁在「每日任務與獎勵」卡片的徽章清單下方提供「🎁 查看等級獎品」按鈕，點擊後以彈出視窗（手機上會以底部抽屜樣式顯示）呈現完整 10 個里程碑清單、目前等級與距離下一個獎品還差幾級，可用關閉按鈕、點擊視窗外側或按 Esc 關閉，不會在首頁直接展開佔用版面。家長頁另外顯示累計已解鎖金額與已確認發放金額，達成新里程碑時會有明確的鼓勵提示文字。
- 💾 **本機學習紀錄**：使用 `localStorage` 保存既有版本化單元紀錄，並新增 `semesterProgress` 保存關卡三段完成集合、題目統計、近期題目與已取得單元獎勵。學期 XP 由完成集合重新計算而非直接累加，因此重複操作或跨裝置合併不會重複發獎。舊資料首次載入時只補空白欄位，不會清除原有 `rewards.xp`、錯題、星星或單元進度。
- 👨‍👩‍👧 **家長進度檢視頁**（`parent.html`）：預設家長密碼為 `8888`。康軒版顯示 79 關完成數、各單元進度與獎勵、六上 XP、目前關卡，以及依關卡正確率排序的弱點主題；不顯示預定進度、落後或完成日期預測。既有錯題、等級獎品與家長確認功能完整保留。
- ☁️ **選用的雲端同步（第三階段）**：可選擇性設定 Firebase（Auth + Firestore），讓親子在不同電腦用同一組帳號登入後看到同一份學習進度，教材版本偏好、各版本進度資料與**等級獎品的已解鎖／已確認里程碑**都會一併同步（合併採聯集，不會因為登入而遺失任一裝置已確認的獎品紀錄）。**這是純粹的加分功能**：不設定、未登入、離線或 Firebase 發生任何錯誤時，網站會自動且安全地退回目前的 localStorage 離線模式，首頁／練習／教學／獎勵／家長頁（含 8888 密碼鎖）都與離線版完全一致，不會出現任何錯誤畫面。詳細設定步驟見 [`FIREBASE_SETUP.md`](./FIREBASE_SETUP.md)。

## 專案結構

```
index.html          # 首頁
practice.html        # 練習頁（以 ?unit=<id> 參數決定單元，版本由單元 id 自動判斷）
lesson.html          # 教學模式頁
parent.html          # 家長進度檢視頁
css/styles.css        # 全站樣式（響應式、親子友善配色，含版本切換按鈕樣式）
js/
  data.js             # 翰林版單元清單與題庫資料（完整保留、未變動）
  data-kangxuan.js     # 康軒版《國民小學數學習作》六上單元／小節目錄
  course-stages.js      # 康軒六上 11 大項、79 關與 XP 配置
  stage-question-engine.js # 79 關動態題庫、難度與抽題
  stage-progress.js     # 關卡解鎖、冪等 XP、單元獎勵與同步合併
  lessons.js          # 翰林版各單元教學內容
  lessons-kangxuan.js  # 康軒版各單元基礎教學內容（第1單元完整教學；第2單元沿用翰林版分數除法教學）
  curriculum.js         # 教材版本抽象層：依版本取得單元清單／教學內容／題庫/單元反查版本
  state-shape.js        # 學習狀態資料結構定義與正規化（含版本、進度、獎勵）、舊資料相容遷移
  question-engine.js  # 動態題目產生、隨機抽題、難度與智慧練習
  rewards.js          # XP、徽章、每日任務與等級稱號（保留星星相容欄位但 UI 不顯示）
  logic.js            # 純函式邏輯（分數解析、批改、正確率、連續天數、進度、錯題本）
  storage.js          # localStorage 存取層（版本感知：教材版本偏好、依版本分開的單元進度/錯題本）
  version-ui.js         # 首頁與家長頁共用的版本切換按鈕元件
  parent-auth.js       # 家長密碼設定與驗證邏輯（預設密碼集中管理）
  firebase-config.example.js # Firebase 設定範本（可 commit，內容為佔位字串）
  firebase-config.js  # 真實 Firebase 設定（已 gitignore，需自行複製範本並填入）
  firebase-config-status.js # 判斷 Firebase 設定是否有效的純函式
  sync-logic.js        # 雲端同步純邏輯：狀態合併規則（含版本偏好與分版本進度）、同步 payload、狀態文字、錯誤訊息轉繁中
  cloud-sync.js         # 瀏覽器端 Firebase Auth/Firestore 串接（CDN 動態載入，未設定時不發任何網路請求）
  sync-manager.js       # 協調 storage.js 與 cloud-sync.js：登入合併、答題/清空即時同步、狀態廣播
  ui-logic.js           # 首頁 UI 決策純函式（帳號同步收合規則、Email 遮蔽、等級獎品下一里程碑計算）
  home.js             # 首頁互動邏輯（含版本切換、帳號與雲端同步收合 UI、等級獎品彈出視窗）
  practice.js          # 練習頁互動邏輯（依單元 id 自動判斷版本）
  wrong-review.js      # 將版本化錯題本安全轉為單題／整份複習題目
  lesson.js            # 教學頁互動與閱讀紀錄（依單元 id 自動判斷版本）
  parent.js            # 家長頁互動邏輯（含版本切換、雲端同步狀態顯示）
tests/
  logic.test.js        # 針對 logic.js 的單元測試（Node 內建測試框架）
  storage.test.js       # 針對 storage.js 的單元測試（以記憶體版 localStorage 模擬）
  storage-sync.test.js  # storage.js 狀態變更通知、resetState 清空同步、replaceState 合併寫回、舊資料遷移
  curriculum.test.js     # 教材版本抽象層：預設版本、版本清單、依版本取單元/題庫/教學、狀態正規化與遷移
  question-engine.test.js # 動態題庫、難度篩選、避免重複、智慧練習
  rewards.test.js      # 每日任務、XP、星星、教學閱讀獎勵、等級獎品里程碑（金額、解鎖、確認領取、重複確認防呆）
  course-stages.test.js # 79 關配置、790 題最低作答量與 9900 XP
  stage-question-engine.test.js # 79 關全難度題庫、至少 3950 種變化與抽題去重
  stage-progress.test.js # 解鎖、一次性 XP、Lv.100、migration 與雲端合併
  stage-pages.test.js   # 首頁地圖、家長摘要與新舊任務顯示回歸
  parent-headless-harness.html # 8888 授權後家長關卡摘要瀏覽器驗證
  stage-browser-harness.html # 11 大項各一關實際練習頁瀏覽器驗證
  lesson.test.js       # 確認所有單元都有教學內容
  parent-auth.test.js   # 家長密碼驗證邏輯
  firebase-config-status.test.js # Firebase 設定偵測（佔位字串/缺欄位/完整設定）
  sync-logic.test.js    # 雲端同步合併規則（含版本偏好合併、舊資料遷移）、payload、狀態文字、錯誤訊息
  ui-logic.test.js      # 帳號同步收合規則、Email 遮蔽、等級獎品下一里程碑計算（純函式）
  html-structure.test.js # 靜態檢查首頁/家長頁標記：等級獎品入口位置、彈出視窗預設隱藏、學生端無確認控制項
  wrong-review.test.js # 錯題複習整份／單題選擇、舊資料安全處理
package.json          # 測試指令設定（無任何 npm 相依套件）
```

## 本機執行方式

本站為純靜態網站，但由於使用了 ES Modules（`<script type="module">`），瀏覽器基於安全性限制，**不允許直接以 `file://` 方式開啟 index.html**，需要透過本機伺服器：

**方式一：使用 Python（大多數環境已內建）**

```powershell
cd 專案資料夾
python -m http.server 8080
```

然後開啟瀏覽器造訪 <http://localhost:8080/>

**方式二：使用 Node.js**

```powershell
npx serve .
```

**方式三：使用 VS Code 的 Live Server 擴充套件**

在 `index.html` 上按右鍵選擇「Open with Live Server」。

## 執行測試 / Build 驗證

專案不需要任何建置流程（沒有打包器、沒有 npm 相依套件），但提供了 Node.js 內建測試框架（`node --test`）驗證核心邏輯（分數解析、批改、正確率、連續學習天數、單元進度、錯題本合併與 localStorage 紀錄）：

```powershell
npm test
# 或直接：
node --test tests/
```

練習頁不再讓孩子選擇「基礎／進階／挑戰」。每次固定產生適合六年級的 10 題混合題組，由引擎配置 3 題基礎結構、4 題反推與換算、3 題生活應用，並涵蓋直接解題、選擇及判斷三種認知操作；內部 `difficulty` 欄位只為舊紀錄與題庫相容，不會顯示或由網址控制。

目前共有 180 項 Node 測試，另有 HTTP 與 Edge headless browser 驗證。涵蓋分數判定、localStorage 相容、教材版本切換、79 關配置、各大項 790 題最低完整作答量、全關卡 easy／medium／hard 各 50 題（共 11850 組抽樣，超過至少 3950 種變化需求）、topic operation contract、79 關 hard 各 50 題的單一主角／禁用套語／主題語意／條件完整性稽核、三種內部題型層次的答案洩漏、選擇題唯一數學正解與無關算術拼接防線、79 關固定十題混合題組的結構指紋與認知操作配額、PDF候選題亂碼阻擋／來源metadata／答案驗算／唯一正解／品質門檻／結構去重、分數／小數／比／圓／速率／比例尺答案不變量、同單元跨關卡題型差異、同次 10 題去重、9900 XP、Lv.100、順序解鎖、重複不發獎、舊 XP 保留、semesterProgress migration／Firebase 聯集合併、首頁四種關卡狀態，以及家長通過 8888 後的 79 關與單元獎勵摘要。雲端同步測試全程使用假資料驗證純函式邏輯，**不會連線到真實 Firebase**。

### PDF 候選題庫

家長提供的 PDF 只作為候選來源，不會直接顯示給學生。可重複執行：

```powershell
node scripts\import-candidate-bank.mjs "C:\path\to\候選題庫.pdf"
```

流程會產生 `data/candidate-bank/import-report.json` 與 `REVIEW_REPORT.md`。只有完成來源標記、關卡分類、答案驗算、選項唯一性、語意／教學品質和結構去重，且 `reviewStatus=approved` 的題目才可進入候選池；亂碼或未審核內容一律阻擋。

另新增錯題複習回歸測試，涵蓋單題／整份重答、答對移除、答錯更新、康軒／翰林版本隔離與空錯題本；並驗證每日任務完成後內部 `rewards.stars` 仍正確累加，但首頁、練習頁及家長頁的可見內容不含星星文字或圖示。

## 雲端同步（選用功能）

第三階段加入了「選用的」Firebase 雲端同步，讓親子可以在不同電腦用同一組帳號登入後看到同一份學習進度。重點原則：

- **離線優先**：完全不設定 Firebase 也能正常使用全部功能，這是預設狀態。
- **優雅回退**：只要沒有設定、設定不完整、網路不通或 Firebase 發生任何錯誤，網站一律安全退回目前的 localStorage 離線模式，不會出現錯誤畫面，首頁會顯示「離線模式，尚未設定雲端同步」。
- **不覆蓋、不遺失**：登入時會把本機與雲端資料「合併」而非覆蓋——單元進度取較大值、已完成題目取聯集、錯題本與徽章取聯集、連續天數與每日任務取較新/較大值。
- **狀態透明**：首頁「帳號與雲端同步」卡片與家長頁上方的同步狀態列，隨時顯示目前是離線、已登入、同步中還是發生錯誤。首頁在**已登入且成功同步過**之後，會把完整登入表單收合成一行精簡卡片（顯示遮蔽後的帳號、最近同步時間），並提供「⚙️ 管理帳號與同步」按鈕可隨時展開完整表單（例如登出）；未設定 Firebase、尚未登入或同步發生錯誤時，一律維持完整表單與明確錯誤訊息，不會被誤縮起來，登出後也會自動回復成完整登入表單。
- 完整設定步驟（建立 Firebase 專案、啟用 Email/Password、建立 Firestore 與安全規則、部署注意事項）請見 [`FIREBASE_SETUP.md`](./FIREBASE_SETUP.md)。

## 部署到 GitHub Pages

1. 將本專案推送到 GitHub repository（例如 `linyufu0519/YUE-Learning`）。
2. 到 GitHub repository 的 **Settings → Pages**。
3. 在 **Build and deployment** 選擇 **Source: Deploy from a branch**。
4. **Branch** 選擇要發佈的分支（例如 `main`）與資料夾 `/ (root)`，儲存。
5. 等待數分鐘後，即可透過 `https://<使用者名稱>.github.io/<repo名稱>/` 造訪網站。

### 部署注意事項

- 本站所有連結皆使用**相對路徑**（例如 `css/styles.css`、`js/data.js`、`practice.html?unit=...`），無論部署在 repo 根目錄或子路徑下皆可正常運作，不需額外設定 `base` 路徑。
- 若要發佈非 `main` 分支的內容，請先建立 Pull Request 合併到指定的發佈分支，或直接調整 GitHub Pages 設定中選擇的分支。
- 學習紀錄與錯題本儲存在使用者「當次瀏覽器」的 `localStorage`：
  - 換裝置、換瀏覽器或清除瀏覽器資料都會導致紀錄消失（若已設定並登入 Firebase 雲端同步則不受影響）。
  - 舊版紀錄會自動補上第二階段新增的 `rewards`、教學閱讀與最近出題欄位，不需要手動清除資料。
  - 本次新增教材版本功能後，舊資料會自動視為翰林版進度並保留完整內容；教材版本偏好預設為康軒版（不影響舊資料本身）。
- GitHub Pages 為純靜態託管，不需要任何伺服器端設定或環境變數。
- 家長專區預設密碼集中定義於 `js/parent-auth.js` 的 `DEFAULT_PARENT_PASSWORD`，預設值為 `8888`。這是離線純前端的防誤入機制，密碼可從網站原始碼查到，不能取代後端身分驗證；若需要真正的存取控制，必須導入後端驗證。
- 雲端同步的 `js/firebase-config.js` 已列入 `.gitignore`，一般 `git push` 不會把真實金鑰帶到遠端；若要讓正式部署的 GitHub Pages 也能使用雲端同步，需要額外操作，詳見 `FIREBASE_SETUP.md`「步驟六」。未做這一步時，正式站台會維持離線模式，功能完全不受影響。

## 後續可擴充方向

- 依相同的 `question-engine.js` 架構，陸續補上其他單元（比與比值、圓的周長與面積等）的動態題庫。
- 增加語音朗讀題目、圖形化解題步驟等無障礙／低年級友善功能。
- 若需要多位小孩各自獨立的帳號與進度（目前 Firebase 同步為單一共用帳號），可在現有 `users/{uid}/learning/state` 結構上擴充成多位小孩子文件。
- 康軒六上 79 關題庫已全數開放；後續可持續增加新的生活情境與挑戰題型。
